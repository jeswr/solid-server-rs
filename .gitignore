/target
# Cargo.lock IS committed (reproducible builds for the experimental binary) — keep it tracked.
**/*.rs.bk
*.pdb
.DS_Store

# Conformance harness generated reports (EARL/HTML/log) — regenerate with conformance/run.sh
/conformance/reports/

# Bench harness DEV artifacts (regenerate with bench/run.sh)
/bench/tls/
/bench/results/
